# Google OAuth Setup Instructions
# ================================

# STEP 1: CREATE GOOGLE OAUTH CREDENTIALS
# ---------------------------------------
# 1. Go to: https://console.cloud.google.com/
# 2. Create new project or select existing
# 3. Navigate to "APIs & Services" > "OAuth consent screen"
# 4. Configure consent screen (choose "External")
# 5. Go to "APIs & Services" > "Credentials"
# 6. Click "Create Credentials" > "OAuth Client ID"
# 7. Choose "Web application"
# 8. Add these Authorized redirect URIs:
#    For Local Development:
#    - http://localhost:8000/auth/google/callback
#    
#    For Production (kamotech.net):
#    - https://kamotech.net/auth/google/callback
#    - https://www.kamotech.net/auth/google/callback
# 9. Copy your Client ID and Client Secret

# LOCAL DEVELOPMENT (XAMPP)
# -------------------------
GOOGLE_CLIENT_ID=your-local-client-id
GOOGLE_CLIENT_SECRET=your-local-client-secret
GOOGLE_REDIRECT_URI=http://localhost:8000/auth/google/callback

# PRODUCTION (kamotech.net on Hostinger)
# -------------------------------------
GOOGLE_CLIENT_ID=your-production-client-id
GOOGLE_CLIENT_SECRET=your-production-client-secret
GOOGLE_REDIRECT_URI=https://kamotech.net/auth/google/callback

# HOSTINGER DEPLOYMENT (Manual Upload Method)
# ------------------------------------------
# BEFORE UPLOADING:
# 1. Build assets locally:
#    npm install
#    npm run build
# 2. Update .env file with production values
# 3. Clear local caches:
#    php artisan config:clear
#    php artisan cache:clear
#
# UPLOAD FILES:
# 1. Upload all project files via FTP/File Manager
# 2. Make sure /public folder is your document root
#    - In hPanel: Domains > Manage > Document Root
#    - Set to: /domains/kamotech.net/public_html/public
#
# AFTER UPLOADING:
# 1. Update .env file in Hostinger File Manager:
#    APP_NAME=Kamotech
#    APP_ENV=production
#    APP_DEBUG=false
#    APP_URL=https://kamotech.net
#    
#    DB_CONNECTION=mysql
#    DB_HOST=localhost
#    DB_PORT=3306
#    DB_DATABASE=your_database_name
#    DB_USERNAME=your_database_user
#    DB_PASSWORD=your_database_password
#    
#    GOOGLE_CLIENT_ID=your-production-client-id
#    GOOGLE_CLIENT_SECRET=your-production-client-secret
#    GOOGLE_REDIRECT_URI=https://kamotech.net/auth/google/callback
#
# 2. SSH into Hostinger and run:
#    cd /home/your-username/domains/kamotech.net/public_html
#    php artisan migrate --force
#    php artisan storage:link
#    php artisan config:cache
#    php artisan route:cache
#    php artisan view:cache
#
# 3. Set permissions via SSH:
#    chmod -R 755 storage
#    chmod -R 755 bootstrap/cache
#
# IMPORTANT NOTES:
# ---------------
# - Use SAME Google project but DIFFERENT OAuth credentials for dev/prod
# - Production redirect URI must use https://
# - Test in incognito mode first
# - Check storage/logs/laravel.log if errors occur
